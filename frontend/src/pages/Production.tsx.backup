import { useState } from 'react'
import {
  Clapperboard, Plus, DollarSign, Users, Calendar, Film, FileText,
  Package, Building2, BarChart3, Shield, MessageSquare, Cloud,
  Clock, TrendingUp, AlertCircle, Camera, Mic, MapPin, FileSignature
} from 'lucide-react'

type Tab = 'projects' | 'budget' | 'team' | 'schedule' | 'postprod' | 'contracts' | 'equipment' | 'crm' | 'reports' | 'security' | 'communication' | 'integrations'

export default function Production() {
  const [activeTab, setActiveTab] = useState<Tab>('projects')

  const tabs = [
    { id: 'projects' as const, label: 'Proje Yönetimi', icon: <Clapperboard size={18} /> },
    { id: 'budget' as const, label: 'Bütçe & Finans', icon: <DollarSign size={18} /> },
    { id: 'team' as const, label: 'Ekip & Oyuncu', icon: <Users size={18} /> },
    { id: 'schedule' as const, label: 'Çekim Planlama', icon: <Calendar size={18} /> },
    { id: 'postprod' as const, label: 'Post Prodüksiyon', icon: <Film size={18} /> },
    { id: 'contracts' as const, label: 'Sözleþmeler', icon: <FileSignature size={18} /> },
    { id: 'equipment' as const, label: 'Ekipman', icon: <Package size={18} /> },
    { id: 'crm' as const, label: 'Müþteri & Ajans', icon: <Building2 size={18} /> },
    { id: 'reports' as const, label: 'Raporlama', icon: <BarChart3 size={18} /> },
    { id: 'security' as const, label: 'Güvenlik', icon: <Shield size={18} /> },
    { id: 'communication' as const, label: 'Ýletiþim', icon: <MessageSquare size={18} /> },
    { id: 'integrations' as const, label: 'Entegrasyonlar', icon: <Cloud size={18} /> },
  ]

  return (
    <div className=\""space-y-6\"">
      {/* Stats */}
      <div className=\""grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6\"">
        <div className=\""bg-white rounded-2xl shadow-sm border border-neutral-200 p-6\"">
          <div className=\""flex items-center justify-between mb-4\"">
            <div className=\""w-12 h-12 bg-purple-100 rounded-xl flex items-center justify-center\"">
              <Clapperboard className=\""text-purple-600\"" size={24} />
            </div>
            <span className=\""text-xs text-neutral-600 font-medium\"">Aktif</span>
          </div>
          <h3 className=\""text-2xl font-bold text-neutral-900 mb-1\"">12</h3>
          <p className=\""text-sm text-neutral-600\"">Devam Eden Proje</p>
        </div>

        <div className=\""bg-white rounded-2xl shadow-sm border border-neutral-200 p-6\"">
          <div className=\""flex items-center justify-between mb-4\"">
            <div className=\""w-12 h-12 bg-green-100 rounded-xl flex items-center justify-center\"">
              <DollarSign className=\""text-green-600\"" size={24} />
            </div>
            <span className=\""text-xs text-green-600 font-medium\"">+12.5%</span>
          </div>
          <h3 className=\""text-2xl font-bold text-neutral-900 mb-1\"">?2.4M</h3>
          <p className=\""text-sm text-neutral-600\"">Toplam Bütçe</p>
        </div>

        <div className=\""bg-white rounded-2xl shadow-sm border border-neutral-200 p-6\"">
          <div className=\""flex items-center justify-between mb-4\"">
            <div className=\""w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center\"">
              <Users className=\""text-blue-600\"" size={24} />
            </div>
            <span className=\""text-xs text-neutral-600 font-medium\"">Çalýþan</span>
          </div>
          <h3 className=\""text-2xl font-bold text-neutral-900 mb-1\"">48</h3>
          <p className=\""text-sm text-neutral-600\"">Aktif Ekip Üyesi</p>
        </div>

        <div className=\""bg-white rounded-2xl shadow-sm border border-neutral-200 p-6\"">
          <div className=\""flex items-center justify-between mb-4\"">
            <div className=\""w-12 h-12 bg-orange-100 rounded-xl flex items-center justify-center\"">
              <Calendar className=\""text-orange-600\"" size={24} />
            </div>
            <span className=\""text-xs text-neutral-600 font-medium\"">Bu Hafta</span>
          </div>
          <h3 className=\""text-2xl font-bold text-neutral-900 mb-1\"">8</h3>
          <p className=\""text-sm text-neutral-600\"">Planlý Çekim</p>
        </div>
      </div>

      {/* Tabs */}
      <div className=\""bg-white rounded-2xl border border-neutral-200 p-2\"">
        <div className=\""grid grid-cols-2 md:grid-cols-4 lg:grid-cols-6 gap-2\"">
          {tabs.map((tab) => (
            <button
              key={tab.id}
              onClick={() => setActiveTab(tab.id)}
              className={lex items-center justify-center gap-2 px-4 py-3 rounded-xl transition-all {
                activeTab === tab.id
                  ? 'bg-neutral-900 text-white shadow-lg'
                  : 'text-neutral-600 hover:bg-neutral-100'
              }}
            >
              {tab.icon}
              <span className=\""font-medium text-sm hidden lg:inline\"">{tab.label}</span>
            </button>
          ))}
        </div>
      </div>

      {/* Content */}
      <div className=\""bg-white rounded-2xl border border-neutral-200 p-6\"">
        {activeTab === 'projects' && <ProjectsContent />}
        {activeTab === 'budget' && <BudgetContent />}
        {activeTab === 'team' && <TeamContent />}
        {activeTab === 'schedule' && <ScheduleContent />}
        {activeTab === 'postprod' && <PostProdContent />}
        {activeTab === 'contracts' && <ContractsContent />}
        {activeTab === 'equipment' && <EquipmentContent />}
        {activeTab === 'crm' && <CRMContent />}
        {activeTab === 'reports' && <ReportsContent />}
        {activeTab === 'security' && <SecurityContent />}
        {activeTab === 'communication' && <CommunicationContent />}
        {activeTab === 'integrations' && <IntegrationsContent />}
      </div>
    </div>
  )
}

function ProjectsContent() {
  return (
    <div className=\""space-y-6\"">
      <div className=\""flex justify-between items-center\"">
        <h2 className=\""text-2xl font-bold text-neutral-900\""> Proje Yönetimi</h2>
        <button className=\""flex items-center gap-2 px-4 py-2 bg-neutral-900 text-white rounded-xl hover:bg-neutral-800\"">
          <Plus size={20} /> Yeni Proje
        </button>
      </div>
      <div className=\""grid grid-cols-1 lg:grid-cols-3 gap-6\"">
        <div className=\""space-y-4\"">
          <div className=\""flex items-center gap-2\"">
            <div className=\""w-3 h-3 bg-yellow-500 rounded-full\""></div>
            <h3 className=\""font-semibold\"">Pre-Production</h3>
            <span className=\""text-xs bg-neutral-100 px-2 py-1 rounded-full\"">3</span>
          </div>
          <div className=\""border rounded-xl p-4\"">
            <h4 className=\""font-semibold\"">Nike Reklam Filmi</h4>
            <p className=\""text-sm text-neutral-600\"">Reklam  30 saniye</p>
            <div className=\""mt-3 space-y-2\"">
              <div className=\""flex items-center gap-2 text-sm\""><Users size={14} /> Ahmet Yýlmaz</div>
              <div className=\""flex items-center gap-2 text-sm\""><Calendar size={14} /> 18-22 Ekim</div>
              <div className=\""flex items-center gap-2 text-sm\""><DollarSign size={14} /> ?450,000</div>
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}

function BudgetContent() {
  return (
    <div className=\""space-y-6\"">
      <h2 className=\""text-2xl font-bold\""> Bütçe & Finans Yönetimi</h2>
      <div className=\""grid grid-cols-1 md:grid-cols-3 gap-6\"">
        <div className=\""bg-gradient-to-br from-green-500 to-emerald-600 rounded-2xl p-6 text-white\"">
          <TrendingUp size={24} className=\""mb-2\"" />
          <h3 className=\""font-semibold\"">Toplam Gelir</h3>
          <p className=\""text-3xl font-bold\"">?2,850,000</p>
        </div>
      </div>
    </div>
  )
}

function TeamContent() {
  return (
    <div className=\""space-y-6\"">
      <h2 className=\""text-2xl font-bold\""> Ekip & Oyuncu Yönetimi</h2>
      <div className=\""grid grid-cols-1 md:grid-cols-4 gap-4\"">
        {[
          { name: 'Yönetmenler', count: 5, icon: <Clapperboard size={20} /> },
          { name: 'Oyuncular', count: 18, icon: <Users size={20} /> },
          { name: 'Kameraman', count: 8, icon: <Camera size={20} /> },
          { name: 'Ses Teknisyeni', count: 6, icon: <Mic size={20} /> },
        ].map((dept, idx) => (
          <div key={idx} className=\""border rounded-xl p-4\"">
            <div className=\""mb-2\"">{dept.icon}</div>
            <h3 className=\""font-semibold\"">{dept.name}</h3>
            <p className=\""text-2xl font-bold\"">{dept.count}</p>
          </div>
        ))}
      </div>
    </div>
  )
}

function ScheduleContent() {
  return (
    <div className=\""space-y-6\"">
      <h2 className=\""text-2xl font-bold\""> Çekim Planlama</h2>
      <div className=\""grid grid-cols-7 gap-4\"">
        {['Pzt', 'Sal', 'Çar', 'Per', 'Cum', 'Cmt', 'Paz'].map((day, idx) => (
          <div key={idx} className=\""border rounded-xl p-4 text-center\"">
            <p className=\""text-xs text-neutral-600\"">{day}</p>
            <p className=\""text-2xl font-bold\"">{13 + idx}</p>
          </div>
        ))}
      </div>
    </div>
  )
}

function PostProdContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Post Prodüksiyon Takibi</h2></div>
}

function ContractsContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Sözleþme & Belgeler</h2></div>
}

function EquipmentContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Ekipman & Envanter Yönetimi</h2></div>
}

function CRMContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Müþteri & Ajans Yönetimi</h2></div>
}

function ReportsContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Raporlama ve Analitik</h2></div>
}

function SecurityContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Yetkilendirme ve Güvenlik</h2></div>
}

function CommunicationContent() {
  return <div><h2 className=\""text-2xl font-bold\""> Ýletiþim ve Koordinasyon</h2></div>
}

function IntegrationsContent() {
  return (
    <div className=\""space-y-6\"">
      <h2 className=\""text-2xl font-bold\""> Entegrasyonlar</h2>
      <div className=\""grid grid-cols-1 md:grid-cols-3 gap-6\"">
        {[
          { name: 'Google Drive', status: 'Aktif' },
          { name: 'Slack', status: 'Aktif' },
          { name: 'QuickBooks', status: 'Pasif' },
        ].map((integration, idx) => (
          <div key={idx} className=\""border rounded-xl p-6\"">
            <Cloud size={32} className=\""mb-3\"" />
            <h3 className=\""font-semibold\"">{integration.name}</h3>
            <span className={	ext-xs px-2 py-1 rounded-full {integration.status === 'Aktif' ? 'bg-green-100 text-green-700' : 'bg-neutral-100'}}>
              {integration.status}
            </span>
          </div>
        ))}
      </div>
    </div>
  )
}
